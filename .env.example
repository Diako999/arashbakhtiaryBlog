SECRET_KEY=change-me
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
DATABASE_URL=mysql://USER:PASSWORD@localhost:3306/prodblog_dev

# Set a unique, hard-to-guess path in production — stock Django admin is
# kept installed as a technical fallback but must not live at /admin/.
ADMIN_URL=manage-portal/

# prod.py only:
# EMAIL_HOST=
# EMAIL_PORT=587
# EMAIL_HOST_USER=
# EMAIL_HOST_PASSWORD=
